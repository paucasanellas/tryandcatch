# Composables

Convención de composables. Documento incremental: se amplía cuando haya más convención real.

- Fichero sin prefijo: `app/composables/navigation.ts`, no `useNavigation.ts`.
- La función exportada sí lleva el prefijo: `export function useNavigation() {}`.
